# Protocols read

Which documents this mod's session has read, in which version, and which were of no use, so that it does not
reread them when they have not moved (`WELCOME.md`, point 5). Read on **2026-09-25**, after a compaction of the
session's context. The version is the last commit that touched the file (`git log -1 --format='%h %ad'`), plus
the first 12 hex digits of its SHA-256 and its size, which say whether it moved since. A file that reads
`modified, not committed` in `git status` is marked so. Reread a file only if its hash changed.

All monorepo files below were clean in `git status` when read.

## Monorepo and shared repositories

| File | Version read | Useful? | What was taken from it, or why not |
| --- | --- | --- | --- |
| `AGENTS.md` | `90d51374` 2026-09-25, `36631E730433`, 46 lines | yes | evidence rule (keep the latest report per scenario, minify, delete own archive, one line per run in `docs/runs/`); publishing by CI rules that never bend |
| `AUDIT.md` | `90d51374` 2026-09-25, `F46FE88E5EC0`, 232 lines | yes | never launch the Windows game; request runs through `Submit-PickleRun.ps1`, no watcher, no edit of the mod tree while a request is pending; the gates `done -> tested -> prepublished -> published`; **fail fast policy** (what is checked before `publish`, what is lifted, rollback target chosen first); `TESTING.md` must state the passes; `@review` captures are opened; buttons are named by translation key |
| `PUBLISHING.md` | `90d51374` 2026-09-25, `D3660C50CF84`, 683 lines | yes (sections: description, thanks and register, images and gallery, mise en production, updates, CI) | one gallery folder with only the numbered images; **change note starts with `[b]<version>[/b]` alone on the first line**; thanks are checked against the global register; every named or exercised integration is thanked and linked in the description; git and commit rules (English, pathspec on commit, read `git status` after) | 
| `TRANSLATIONS.md` | `90d51374` 2026-09-25, `3368579D01DC`, 100 lines | yes, for one point | relevant files that change reset the translation fields to `unchecked` until revalidated: the tip wording changed on 2026-09-25 and `STATUS.md` must say so |
| `STYLE_RIMWORLD.md` | `90d51374` 2026-09-25, `DE13CBE5E1F9`, 484 lines | **no** | Preview and ModIcon style, generated by the owner alone; nothing to do for the four Workshop pictures, which are game captures, not the Preview. Reread only if a Preview or an icon is asked for |
| `scripts/SEARCHING.md` | `90d51374` 2026-09-25, `9DBD52B2BCD4`, 168 lines | **no** | searching the mod corpus; this mod is original and no port question is open. Reread if a defName collision or a class check across mods is needed |
| `PickleTools/README.md` | `2b7b6d0` 2026-09-25, `6EA974180EB8`, 84 lines | partly | the table of companions: `KeyedClick` (click a button by its translation key) and `HoverSteps` (assert a real tooltip) are not used yet by this suite |
| `PickleTools/Headless/README.md` | `b2712fc` 2026-09-25, `988DBF0DCEE7`, 487 lines | partly | filter terms, passes and `-DepMap`, `-Then`, `-EvidenceDir`, exit codes, the pointer that rests at the screen centre; the launcher internals, sleep, lock and reservation sections were of no use to a session that only files requests |
| `PickleTools/Docs/steps.md` | no commit found, `61750ECA84D2`, 217 lines | partly | the steps of the companions; only ScreenshotMode, ScreenshotStudio, FilmTicks, RimmsqolSteps and KeyedClick matter here |
| `Rimworld-Release-Admin/docs/OPERATIONS.md` | `d403592` 2026-09-25, `6F556DE4BBF7`, 207 lines | partly | dry-run first and read, generator command and its options (`--gallery-dir`, `--description-file`), what a publish sends, the gallery stays manual, Virginie's three manual steps for a 1.0.0; the credentials and Codespace sections were of no use |
| `Rimworld-Ticket-Dispatcher/docs/WELCOME.md` | `79668cc` 2026-09-25, `B9F93A680F17`, 97 lines | yes | one request = one pass; small tickets; a request carries no SHA, so **no change to the mod tree until `RUN_DONE`**; delete evidence with `robocopy /MIR`; keep no `report.html`; note the version of each doc read |
| `Rimworld-Ticket-Dispatcher/docs/SUBMIT.md` | `79668cc` 2026-09-25, `9AC5E37BB64C`, 131 lines | yes | `-DepMap` is the **file name alone**, a relative path with a separator reads no map; `-Extra` flags; exit codes; a request is not a validation |

## This repository

| File | Version read | Useful? | Notes |
| --- | --- | --- | --- |
| `STATUS.md` | modified 2026-09-25 00:04, `E145AA14354E`, 534 lines | yes, but stale | most of it is history; the front matter and `remaining` need a rewrite (see below) |
| `README.md` | 2026-09-24 23:21, `D2558A142DAB`, 207 lines | yes | says "Interactive RIMMSQOL validation is still pending", which is no longer true; the "does not do reproduction" line stays true for 1.0.0 |
| `CHANGELOG.md` | 2026-09-25 13:16, `BFE3BAEF9900`, 112 lines | yes | mine; `## Unreleased` to be dated in the final commit |
| `ATTRIBUTION.md` | 2026-09-13 02:16, `946B1DA3ACC9`, 74 lines | yes | Harmony and RIMMSQOL credited; PickleTools, Pickle and RimLogging are not named there |
| `LICENSE` | 2026-09-19 19:28, `AE6AE5FA894C`, 21 lines | **no** | MIT, unchanged; not reread |
| `PUBLICATION.md` | 2026-09-25, mine | yes | corrected today: change note, thanks, fail fast, register |
| `Mod/About/About.xml` | 2026-09-24 23:21, `CE250B102C65`, 83 lines | yes | its description must be kept in step by hand with `PUBLICATION.md` section 1: the thanks lines added today are not in it yet |
| `Tests/Pickle/README.md` | 2026-09-22 23:30, `AC32170CB18D`, 65 lines | yes, stale | lists 15 features and says the suite "has not been executed yet"; `TESTING.md` is the current statement |
| `docs/runs/README.md` | mine | yes | evidence retention |
| `docs/MANUAL-REVIEW.md`, `docs/runs/2026-09-2*.md` | mine | yes | |
| `BACKLOG.md` | created 2026-09-25, mine (moved from `docs/` the same day) | yes | the mod's own, not the monorepo's |
| `TESTING.md` | created 2026-09-25 | yes | was missing; `AUDIT.md` requires it |
| `NOTES.md`, `BUGS.md` | **do not exist** | | not created: nothing to put in them |

## Messages from other sessions that change a rule

- **CI/CD setup, 2026-09-25** (`Rimworld-Release-Admin` `f196148`, `docs/OPERATIONS.md` "Changing where the Steam
  description comes from"; `PUBLISHING.md` of Rimworld-protocols `16f3c59`): one source for the Workshop description,
  written once in Markdown in a fenced block under `## Steam description` of `PUBLICATION.md`; the CI converts it to
  BBCode and generates the `<description>` of `About.xml` from it (no hand-kept copy) and stops on any drift. Nothing
  forced; adopt at the next publication or when the owner asks. Here the description is still a BBCode block under
  `## 1.` with a hand-kept plain copy in `About.xml`: adopting means writing the Markdown block, regenerating the
  workflow with `--description-markdown PUBLICATION.md --description-heading '^## Steam description$'
  --about-from-description --replace`, then `sync-about-description.mjs --write` and reading the diff. It changes the SHA,
  so a new dry-run. Same message: the CI refuses a change note whose first line does not carry the version
  (`[b]1.0.0[/b]`, already the case here), and `dispatch-publish.sh` refuses without a reviewer and both secret names
  on `steam-production`. Do not edit `.github/` by hand.
